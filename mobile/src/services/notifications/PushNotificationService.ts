/**
 * FCM registration + foreground/background/killed-state handlers, including the
 * `NORMAL` vs. `HELPER_ALARM` priority branch that routes into
 * `features/helper/services/HelperAlarmService` (SDD §1.4c, §4, §5.7). Phase 11 work.
 */
export {};

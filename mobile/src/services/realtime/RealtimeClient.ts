/**
 * STOMP-over-WebSocket client for `/topic/emergencies/{id}` (SDD §1.4d, §5.8a).
 * Connect/subscribe logic, JWT-on-CONNECT, and the polling fallback are Phase 4b/12b
 * work — this file exists so `services/realtime` shows up in the tree per SDD §4
 * ahead of that.
 */
export {};

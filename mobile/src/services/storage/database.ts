/**
 * SQLite schema/init for the offline-safe emergency event queue (SDD §1.4, §4) — the
 * mechanism that lets `EmergencyLocalStore` persist a trigger even with zero
 * connectivity. Phase 8 work, alongside the emergency workflow that depends on it.
 */
export {};

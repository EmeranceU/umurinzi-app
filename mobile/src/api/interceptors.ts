/**
 * Attach-JWT / refresh-on-401 / retry-queue interceptors for `api/client.ts`
 * (SDD §4). Needs a working auth module (Phase 1 backend, Phase 6 mobile) to attach
 * or refresh a token against — not implemented in this scaffold.
 */
export {};

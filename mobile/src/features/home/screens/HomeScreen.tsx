import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export function HomeScreen() {
  return (
    <PlaceholderScreen
      title="Home"
      note="Device status card + manual SOS button. Phase 8 (SDD §7)."
    />
  );
}

/**
 * {@code FcmPushService} — sends Helper alerts as high-priority, data-only FCM
 * messages so the client controls presentation, not the OS tray (SDD §1.3, §1.4c, §3).
 * Phase 3 work.
 */
package com.umurinzi.emergency.notification.push;

/**
 * {@code SmsProviderClient} interface plus the Africa's Talking / Twilio
 * implementations and the delivery-status webhook controller (SDD §1.3, §1.6, §3, §6).
 * Phase 3 work.
 */
package com.umurinzi.emergency.notification.sms;

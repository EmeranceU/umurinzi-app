/**
 * Channel-strategy senders fanned out from {@code NotificationDispatcher}: one
 * {@code NotificationChannelSender} implementation per channel (push, SMS, and the
 * future email/call stubs) (SDD §1.6, §3). Phase 3 work.
 */
package com.umurinzi.emergency.notification.channel;

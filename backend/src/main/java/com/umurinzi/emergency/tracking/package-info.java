/**
 * Public, token-scoped tracking link for SMS-only (unregistered) contacts (SDD §1.6,
 * §5.10, §6): {@code TrackingTokenService} and the unauthenticated, rate-limited
 * {@code PublicTrackingController}. Phase 4 work.
 */
package com.umurinzi.emergency.tracking;
